<?php foreach ($modelos as $auto): ?>
    <div>
        <a href="detalle/<?php echo htmlspecialchars($auto->id_modelo); ?>">
            <?php echo htmlspecialchars($auto->nombre_modelo); ?>
        </a>
    </div>
<?php endforeach; ?>
